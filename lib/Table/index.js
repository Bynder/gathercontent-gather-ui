import TableColumn from './Column';
import TableHeading from './Heading';
import TableHeadingTitle from './Heading/Title';

const Table = {
  Column: TableColumn,
  Heading: TableHeading,
  HeadingTitle: TableHeadingTitle,
};

export default Table;
